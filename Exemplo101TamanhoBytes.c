/******************************************************************************

                            Online C Compiler.
                Code, Compile, Run and Debug C program online.
Write your code in this editor and press "Run" button to compile and execute it.

*******************************************************************************/
#include <stdio.h>

int main() {
    printf("O tamanho de um inteiro (int) em bytes é: %zu\n", sizeof(int));
    printf("O tamanho de um caractere (char) em bytes é: %zu\n", sizeof(char));
    printf("O tamanho de um float em bytes é: %zu\n", sizeof(float));
    printf("O tamanho de um float em bytes é: %zu\n", sizeof(double));
    printf("O tamanho de um float em bytes é: %zu\n", sizeof(long double));    
    
    return 0;
}
